mergeInto(LibraryManager.library, {
  // Compatibility stub for older WebGL builds that may still import this symbol.
  // Intentionally no-op: no ads, no SDK calls.
  ShowYandexFullscreenAd: function () {}
});
